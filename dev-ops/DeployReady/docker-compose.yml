services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: kora-analytics-api:latest
    ports:
      - "3000:${PORT}"
    env_file:
      - .env
    environment:
      - PORT=${PORT}
